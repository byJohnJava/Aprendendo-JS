const [a, b, c, d] = [10, 8, 9, 6]

const soma = a + b + c
const subtracao = a - d
const multiplicacao = c * a * b
const divisao = d / 2
const modulo = a % b
const exponenciacao = a ** 2

console.log(soma, subtracao, multiplicacao, divisao, modulo, exponenciacao)
